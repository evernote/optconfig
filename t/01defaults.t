#!perl

use Test::More::Behaviour qw(no_plan);

BEGIN {
    use_ok('Optconfig');
}

our $domain = 'test-example';

sub with_optspec {
    my ($spec) = 

describe 'Optconfig' => sub {

    context 'with defaults' => sub {
        Optconfig->new(
    }

}
